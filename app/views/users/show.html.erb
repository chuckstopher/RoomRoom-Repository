<h1><%= full_name %>  <strong>"<%= @user.user_name.capitalize %>"</strong></h1>
<% if @user == current_user %>

<% else %>
<%= link_to ("Message This User"), new_conversation_path(@user), class: "btn btn-large btn-primary" %>
<% end %>

         <table class="table table-hover">
             <p>Renter Info</p>
      <p><strong>Does <%=@user.user_name%> have Pets?</strong> <%= @user.user_pets? %></p>
       <p><strong> Are they a Smoker?</strong> <%= @user.user_smoke? %></p>
        </tbody>
        
        <% @user_posts.each do |post| %>
        <p><%= link_to post.title, post_path(post) %></p>
        <td><%= link_to image_tag post.image.url, :size =>'800x800' %></td>
            </tbody>
<% end %> 


        
   